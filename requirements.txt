# Install with: pip install -r requirements.txt
# Torch CPU wheels from PyTorch index; others from PyPI (handled in Dockerfile).
flwr==1.22.0
numpy==2.3.3
pandas==2.3.2
scikit-learn==1.7.2
scipy==1.16.2
PyYAML==6.0.2
torch==2.7.0
# (No torchvision needed for the vector MLP client)